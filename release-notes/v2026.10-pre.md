OPM Flow for Windows — native MSVC build (hybrid MPI + OpenMP) with a Qt GUI workbench.

> **An unofficial build.** OPM Flow is developed by the [Open Porous Media initiative](https://opm-project.org). This is an independent Windows build of it, not a release of the OPM project, and OPM does not support it. Report anything wrong with *this package* here; anything you can also reproduce on Linux belongs upstream with OPM.

**Downloads** — the installer and the portable zip both work for most people; pick whichever you prefer. If you are on a **managed work laptop without administrator rights**, take the Intel MPI zip: it is the one that needs no administrator at any point.
- `OPM-Flow-2026.10-pre-Setup.exe` — installer: adds Start-menu shortcuts. It can now install **for one user without administrator rights** as well as machine-wide, so it at least runs on a managed laptop. The Visual C++ and OpenMP runtimes ship inside the package, so the only external prerequisite is **Microsoft MPI** (the simulator links `msmpi.dll`); the installer installs it for you when run as an administrator. Installing MS-MPI itself always needs an administrator, so a per-user install ends by telling you that and how — if you cannot get one, use the Intel MPI zip below instead.
- `opm-flow-2026.10-pre-win64.zip` — portable: unzip and run `bin\flow-gui.exe`. The Visual C++ and OpenMP runtimes are already in `bin\`; the only thing you might need is **Microsoft MPI** — if it isn't already installed, run `redist\msmpisetup.exe` once (or `winget install Microsoft.msmpi`).
- `opm-flow-2026.10-pre-impi-win64.zip` — **the no-administrator option**, built against the Intel® MPI Library instead of MS-MPI. Unzip it anywhere you can write, then install the MPI runtime once *as yourself*:
  ```
  python -m pip install --user impi-rt
  ```
  That needs no administrator rights, where installing MS-MPI does — which is what otherwise blocks a locked-down machine, since the simulator links an MPI library even for serial runs. `flow-gui.exe` finds that runtime by itself; from a terminal use `bin\flow-impi.cmd DECK.DATA --output-dir=out`, or `bin\flow-impi.cmd -n 4 DECK.DATA ...` to run on 4 ranks. The MPI runtime is **not** bundled: OPM Flow is GPLv3 and Intel MPI is proprietary, so shipping them together raises a licensing question this package does not try to answer.
- `OPM-Flow-2026.10.0.0.msix` — unsigned MSIX (for sideloading/Store experiments; see PACKAGING.md). **Note:** Windows will not enable its **Install** button unless the signing certificate is already trusted on the machine (it shows *"Publisher: Unknown / this app package's certificate could not be verified"*).
- `opm-flow-gui-0.7.1-ubuntu24.04-x86_64.tar.gz` — **Linux** build of the GUI (Ubuntu 24.04 LTS, all tabs enabled). No `flow` is shipped for Linux — see the **Linux** section below.

**Contents:** `flow.exe` (all model variants, including black-oil; built from current OPM master plus the pending Windows patches) and `flow-gui.exe` **0.7.1** — a full workbench:
- **Fits a laptop screen.** The toolbars wrap onto further lines when the window is narrow, instead of demanding one fixed line: laid out that way they set a minimum window width of 2045 px — wider than a 1920 panel — so the window could not be sized to such a screen at all and *Maximise* had nowhere to go. Given the width they still lay out on one line exactly as before.
- **Full speed on modern laptops** — both `flow` and the simulators flow-gui spawns now opt out of Windows "EcoQoS" power throttling, which otherwise clocks background processes down dramatically (a serial Norne measured 1.6× slower without the exemption — including MPI ranks, which Windows always treats as background).
- **Run tab** — job queue with live progress/ETA, Stop/Skip, deck validation (`--enable-dry-run`), re-run of an already-finished queue (after confirmation), View PRT / View DBG / Open result folder, and a developer *Simulator* override to test your own `flow` build. *Run selected* runs only the highlighted decks instead of the whole queue, a **TUNING** checkbox passes `--enable-tuning` (off by default, matching flow's own default), and *Run*/*Validate* offer to save pending deck edits first, since flow re-reads the decks from disk.
- **Summary Plots tab** (renamed from *Results*) — summary-vector plotting straight from `SMSPEC`/`UNSMRY`: grouped/filtered vector picker with human-readable names plus qsummary-style **wildcard filtering** (`WBHP:B*, WOPR*`); click-to-focus **subplots**, each with its own vector selection; multi-case comparison with **renameable** cases whose new name drives the legend; calendar-date X axis by default; refresh-stable tree and zoom; CSV export. Plots refresh live while a run is still writing.
  - **Subplot grids up to 6×8, picked as a shape.** The *Layout* button opens a grid picker you hover to choose rows × columns, rather than a list of presets — a wide screen wants 2×5 or 3×6, and a list long enough to hold those is a list nobody reads. Charts are built as a layout asks for them, so a session that stays on one chart pays for one. Axes thin their tick count to whatever a subplot can label, so a dense grid stays readable instead of shrinking or eliding its labels.
  - **Ctrl+drag one subplot onto another to swap them** — vectors, zoom and legend position travel together, because where a plot sits in a figure is a presentation decision.
  - **Reset zoom, one subplot or all.** The button still clears every subplot; the arrow beside it clears just the focused one, so putting one plot back no longer costs the view on the others.
  - **A case list you can order**, since the list order *is* the plot order — of the curves, the colours and dashes keyed to them, the legend and the CSV columns.
  - **Sizes that follow the plot they are drawn in**: line width, marker size and legend text scale with the subplot, so a 2×2 layout no longer comes out as four crowded plots with a legend eating a third of each. Runs of the same deck are told apart by the part of their path that differs — **every** one of them, however they were loaded, so no case is left saying only the deck's name.
- **Publication-quality figures** — the legend docks to any edge, floats inside the plot (draggable with the mouse), or hides, and is set in bold a size above the axis labels with a line sample drawn heavier than the curve, so colour and case dash read at a glance; *Save figure...* writes a **vector PDF** sized to the figure (7 in wide, no margins, straight into `\includegraphics`) or a **300 dpi PNG** rendered at 3× rather than grabbed off the screen; colourblind-safe palette with round axis ticks, adjustable line width and marker size. Markers show **every data point** in the summary by default — nothing is sampled away, so they are exactly the samples the run wrote — with **hover** reporting the case, date and value; an *every n* box thins them on a dense curve, stepping by whole data points so a drawn marker is always a real `(time, value)` sample. A per-case shape and hollow fill (after the first case) keep curves separable where markers coincide, and with markers on the **legend shows them too** — each entry's sample is that case's shape riding on its line, so an entry looks like the curve it stands for.
- **3D View tab** — the corner-point grid colored by static (INIT) or dynamic (UNRST) properties incl. synthesized SOIL, well trajectories colored by type, and report-step animation with a **scrubbable** step bar: drag it, or click anywhere on it, to jump straight to that step. The orbit is unrestricted, so the model can be turned to show the base of the reservoir, with a *View* menu for Home / Top / Bottom / Side and an auto-framed default view. A *Remove* button drops a case from the list so it does not grow with every job of a long session (removing a case in the Summary Plots tab drops it here too).
- **Deck Editor tab** — section tree over the deck and its INCLUDE files with Eclipse syntax highlighting. Sections **start closed**, so opening a deck shows its eight sections rather than a wall of hundreds of keywords, and a section you open stays open across a rescan instead of folding back up when a file changes on disk. Plus a keyword filter with expand/collapse, find & replace (*Replace all*, case sensitive by default), Undo/Redo, *Toggle comment* (Ctrl+/), reload of files changed outside the GUI, and double-click / right-click on an INCLUDE'd file name to open it; edits are saved to the original files, never a flattened copy.
- **Sessions and projects that come back** — a `.opmproj` file (and, with no project at all, simply closing the GUI) carries the whole working setup, not just the queue: the plotted **cases** and which one is **active**, the vector filters, **what each subplot shows** and how it is drawn (layout, legend placement including a dragged one, line width, markers), the **3D view**'s case and property, the **deck editor**'s open files, the front tab, the window geometry, and each finished job's output directory and outcome — so *View PRT* and *Open folder* still work on last week's runs. Starting flow-gui again picks up where the last session stopped; the 3D grid is only re-read once that tab is looked at, so it costs nothing at startup. Version 1 project files still open.

Fixed along the way, from testing: the output-directory chooser could keep the *Directory:* box on the old name after a new folder was renamed, so *Choose* created a stray empty directory and used it for output; two runs of the same deck now **both** carry a tag saying which run they are, instead of only the second one being tagged; a run written to a **custom output directory** is no longer registered twice and listed under the same folder twice (it reached the GUI in native separators from there and with forward slashes from everywhere else, which compared as two different cases); the 3D step bar used to page towards a click instead of going there, and moving it while a static property was shown did nothing at all; and stepping through a run no longer grows without bound, since the restart reader caches every array it is asked for and cannot evict them one by one — its cache is now dropped wholesale once past a budget, which matters on models with millions of cells.

flow-gui is cross-platform: the same sources build on Windows, Linux and macOS, and the Windows package and the Linux tarball below are the same 0.7.0. Upgrading from the previous pre-release: the GUI executable was renamed from `flow-gui-qt.exe` to `flow-gui.exe`, and saved GUI settings/queue reset once due to the rename.

**Linux**

- **Run the Ubuntu binary** — unpack `opm-flow-gui-0.7.1-ubuntu24.04-x86_64.tar.gz`, install the runtime packages, run:
  ```bash
  sudo apt install libqt6charts6 libqt6openglwidgets6t64 libfmt9
  ./opm-flow-gui-0.7.1-ubuntu24.04-x86_64/flow-gui
  ```
  Built on Ubuntu 24.04 LTS against the distro Qt; expected to also work on later Ubuntu releases.
  The tarball is flow-gui **0.7.1** — the same version, from the same sources, as the Windows package above, so the feature list under **Contents** applies unchanged.
- **Bring your own simulator** — unlike the Windows package, we do not release `flow` for Linux. Install or build OPM Flow following [opm-project.org](https://opm-project.org/?page_id=36) (e.g. the OPM binary packages: `sudo apt install mpi-default-bin opm-simulators`), then point the *Simulator:* field on the Run tab at it once (e.g. `/usr/bin/flow` or your own `.../opm-simulators/bin/flow`) — remembered between sessions.
- **Other distros: build flow-gui yourself** — it is a small standard CMake + Qt 6 project:
  ```bash
  sudo apt install qt6-base-dev qt6-charts-dev libfmt-dev cmake ninja-build g++
  git clone https://github.com/GitPaean/opm_flow_windows
  cmake -S opm_flow_windows/flow-gui -B build-gui -DCMAKE_BUILD_TYPE=Release \
        -DFLOWGUI_OPM_PREFIX=<opm-common install prefix>
  cmake --build build-gui
  ```
  The Results and 3D View tabs need an opm-common install prefix at configure time (watch for `flow-gui summary plotting: ON` / `3D viewer: ON`); without it you still get the Run tab and deck editor. Full recipe including the opm-common step: [flow-gui/README.md → Building → Linux](https://github.com/GitPaean/opm_flow_windows/blob/master/flow-gui/README.md#linux).

**Screenshots** (Norne benchmark case)

| Run queue | Results & case comparison |
|---|---|
| ![Run tab](https://raw.githubusercontent.com/GitPaean/opm_flow_windows/master/docs/screenshots/run-tab.png) | ![Results tab](https://raw.githubusercontent.com/GitPaean/opm_flow_windows/master/docs/screenshots/results-compare.png) |
| **3D viewer** | **Deck editor** |
| ![3D View](https://raw.githubusercontent.com/GitPaean/opm_flow_windows/master/docs/screenshots/viewer-3d.png) | ![Deck editor](https://raw.githubusercontent.com/GitPaean/opm_flow_windows/master/docs/screenshots/deck-editor.png) |

**Getting past SmartScreen (this build is unsigned)**
The binaries are not code-signed, so Windows shows *reputation* warnings — these are not virus detections, and for the `.exe`/`.zip` they are dismissible:
- **When downloading** (Edge/Chrome): if it says *"… isn't commonly downloaded"* and the download stops or shows **Canceled**, open the download's **⋯ (More actions)** menu → **Keep**, then **Keep anyway**.
- **On first launch**: if *"Windows protected your PC"* appears, click **More info → Run anyway**.

**Notes**
- Windows 10/11 x64; the Linux GUI tarball targets Ubuntu 24.04 LTS or later (x86_64).
- Parallel runs, MS-MPI package: `mpiexec -n <ranks> flow.exe DECK.DATA --threads-per-process=<threads> --output-dir=<dir>`, or use the GUI.
- Parallel runs, **Intel MPI package**: use `bin\flow-impi.cmd -n <ranks> DECK.DATA ...`, or the GUI. Not a bare `mpiexec` — if MS-MPI is also installed that name resolves to *its* launcher, which cannot start an Intel MPI binary and fails with `unable to decode hostport …  PMI_Init returned -1`. `flow-impi.cmd` and the GUI both pick the right launcher for you.
- OPM Flow is developed by the [Open Porous Media initiative](https://opm-project.org) ([github.com/OPM](https://github.com/OPM)) under GPLv3+. These binaries include a small set of Windows/MSVC patches maintained in the forks below; the complete corresponding source is the upstream OPM repositories plus the Windows patches on the `windows` branch of [opm-common](https://github.com/GitPaean/opm-common/tree/windows), [opm-grid](https://github.com/GitPaean/opm-grid/tree/windows) and [opm-simulators](https://github.com/GitPaean/opm-simulators/tree/windows). The package's `LICENSE.txt` names the exact **commit** each binary was built from rather than a branch, so it stays correct as the branches move on. The build harness and flow-gui are Copyright (C) 2026 SINTEF Digital, Mathematics & Cybernetics, GPLv3+.
